[build-system]
requires = ["setuptools>=42", 
            "torch~=1.11.0", 
            "torchvision~=0.12.0", 
            "torch-snippets"
            "pycocotools", 
            "numpy", 
            "Pillow",
            "matplotlib"]
build-backend = "setuptools.build_meta"
