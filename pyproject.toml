[build-system]
requires = ["setuptools>=42", 
            "torch~=1.11.0", 
            "torchvision~=0.12.0", 
            "pycocotools", 
            "numpy", 
            "Pillow",
            "matplotlib",
            "torch_snippets"]
build-backend = "setuptools.build_meta"
